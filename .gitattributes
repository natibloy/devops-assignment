# Guests are Linux; CRLF in any shell, Salt, YAML or Jinja file would break the
# parser that reads it. The first line covers every text file, so per-extension
# rules below it would be redundant.
* text=auto eol=lf

*.deb  binary
*.tar  binary
*.bz2  binary
*.gz   binary
