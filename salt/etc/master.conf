interface: 0.0.0.0

# The compute minion's key is trusted on first contact so `vagrant up` needs no
# manual `salt-key -A` step.
auto_accept: True

file_roots:
  base:
    - /srv/salt

pillar_roots:
  base:
    - /srv/pillar

log_level: info
