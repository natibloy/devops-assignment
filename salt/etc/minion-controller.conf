# The controller is itself Salt-managed: it runs a minion against its own master
# so the controller and compute nodes converge through the identical highstate.
id: controller
master: localhost

grains:
  role: controller

log_level: info
