# Slurm reads this file whenever a cgroup-aware plugin is loaded. Constraints are
# left off because the VirtualBox guests run a single job at a time and cgroup v2
# delegation inside the box adds no isolation worth the failure modes.
CgroupPlugin=disabled
ConstrainCores=no
ConstrainRAMSpace=no
ConstrainDevices=no
