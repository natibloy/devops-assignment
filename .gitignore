.vagrant/
*.log

# Build outputs produced by the builder node; the directory itself is tracked.
artifacts/*
!artifacts/.gitkeep

# Python bytecode from running the gateway locally
__pycache__/
*.py[cod]

# Claude Code local settings (machine-specific, not part of the submission)
.claude/settings.local.json
